export default function HomePage() {
  return (
    <main>
      <h1>{ {{APP_NAME_JS}} }</h1>
      <p>{ {{APP_DESCRIPTION_JS}} }</p>
      <p>Prepared for deployment to {{HOSTING_TARGET}}.</p>
    </main>
  );
}
